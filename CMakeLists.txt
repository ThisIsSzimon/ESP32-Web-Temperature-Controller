cmake_minimum_required(VERSION 3.16.0)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(ESP32_Setup)

message(STATUS "WEBUI_DIR=${WEBUI_DIR}")
if(EXISTS ${WEBUI_DIR})
  message(STATUS "SPIFFS webui: directory exists, will be flashed")
  spiffs_create_partition_image(webui ${WEBUI_DIR} FLASH_IN_PROJECT)
else()
  message(STATUS "SPIFFS webui: directory DOES NOT exist")
endif()